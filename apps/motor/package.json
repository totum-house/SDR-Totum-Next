{
  "name": "@sdr-next/motor",
  "version": "0.1.0",
  "private": true,
  "description": "Motor SDR — brain.js + flow_runner + manus_client (Node CJS, PM2)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@supabase/supabase-js": "^2.108.2",
    "axios": "^1.7.9",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
